import { mount } from '../../utils/dom.js';
export default { render(root){ mount(root, `<div class="card" style="padding:1rem"><h2>Templates</h2><a class="btn mt-2" href="/templates/create">+ Create</a><p class="mt-2">TODO: table</p></div>`);} };
