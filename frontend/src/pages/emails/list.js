import { mount } from '../../utils/dom.js';
export default { render(root){ mount(root, `<div class="card" style="padding:1rem"><h2>Emails</h2><a class="btn mt-2" href="/emails/send-batch">Send Batch</a><p class="mt-2">TODO: table + filters</p></div>`);} };
